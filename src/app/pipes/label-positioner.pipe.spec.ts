import { LabelPositionerPipe } from './label-positioner.pipe';

describe('LabelPositionerPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelPositionerPipe();
    expect(pipe).toBeTruthy();
  });
});
